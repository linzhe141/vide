import { createApp } from 'vue'
import App from './App.vue'
const app = createApp(App)
app.mount('#root')

import { createRoot } from 'react-dom/client'

createRoot(document.getElementById('aa')!).render('xxxxxx')
